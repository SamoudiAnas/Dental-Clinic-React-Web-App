import React from "react";
import styled from "styled-components";

function DeleteModal() {
  return <Wrapper>Delete Modal</Wrapper>;
}

export default DeleteModal;

const Wrapper = styled.div`
  position: absolute;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%);
`;
